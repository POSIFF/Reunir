<%= form_for(pauta) do |f| %>
  <% if pauta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pauta.errors.count, "error") %> prohibited this pauta from being saved:</h2>

      <ul>
      <% pauta.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %>
    <%= f.text_field :codigo %>
  </div>

  <div class="field">
    <%= f.label :descricao %>
    <%= f.text_field :descricao %>
  </div>

  <div class="field">
    <%= f.label :autor %>
    <%= f.text_field :autor %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
